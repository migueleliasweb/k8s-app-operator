# k8s-app-operator
Simple operator that to deploy apps in Kubernetes with best practices built-in

## Long term plan

- App
- AppConfig
    - Namespaced overrides
- GlobalAppConfig
    - Global overrides